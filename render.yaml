services:
  - type: web
    name: devbook
    env: docker
    plan: free
    repo: https://github.com/rodriggocode/devbook-api
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromService:
          type: psql
          name: devbook
          property: connectionString

databases:
  - name: devbook
    plan: free
